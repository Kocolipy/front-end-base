import { Home } from "@/pages/home";

/**
 * The application root. There is no router yet — when one arrives, this is the
 * file that owns it, and `src/pages/` is what it routes to.
 */
export function App() {
  return <Home />;
}
